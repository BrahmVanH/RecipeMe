{{!-- html for the header and footer, and spaces to drop in the other templates/template items --}}
<!DOCTYPE html>
<html lang="en">
  
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RecipeMe</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Delius&family=Work+Sans:ital,wght@0,200;0,300;0,400;0,600;0,800;1,200;1,300;1,600;1,700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../public/css/styles.css" />

  </head>

<body>
  <header>
    {{!-- TODO: Create header --}}
      <nav class="navbar">
        {{!-- navbar links that display on the homepage--}}
        {{#if loggedIn}}
        <div class="navbar-brand">
              <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">Hello, {{user.name}}</a>
              <button type="button" class="open-modal" data-open="(make this the id of modal)">Create a Recipe</button>
              {{!-- <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">Create a Recipe</a> --}}
              <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">View Recipes</a>
              <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">Log Out</a>
        </div>
        {{else}}
        <div class="navbar-brand">
              <button type="button" class="open-modal" data-open="(make this the id of modal)">Sign Up</button>
              {{!-- <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">Sign Up</a> --}}
              <button type="button" class="open-modal" data-open="(make this the id of modal)">Log In</button>
              {{!-- <a class="navbar-brand text-dark text-opacity-75 ms-3" href="#">Log In</a> --}}
        </div>
        {{/if}}
      </nav> 
  </header>

  <main>
    {{{ body }}}
  </main>

  <footer>
    {{!-- TODO: Create footer --}}
  </footer>
</body>

</html>